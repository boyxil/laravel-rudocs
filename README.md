Laravel bundle "docs" from standart Laravel PHP framework.

Just put folder 'docs' to Laravel bundles directory **bunlles**.
Then replace your 'laravel/documentation' with **documentation** folder from this.
That's all.
